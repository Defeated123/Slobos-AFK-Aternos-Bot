services:
  bot:
    image: node:22-bookworm-slim
    working_dir: /app
    command: sh -c "npm install && node index.js"
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "3000:5000"
    environment:
      - PORT=5000
    restart: unless-stopped
