services:
  bot:
    image: node:22-bookworm
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    command: sh -c "npm install && node index.js"
    restart: unless-stopped
